# Where the client reads incident data from:
#   unset / "mock" — synchronous read of the seeded deterministic dataset (default)
#   "api"          — fetch from this app's own /api/incidents route (same data today;
#                    proves the API contract the future backend will implement)
# NEXT_PUBLIC_DATA_SOURCE=api
